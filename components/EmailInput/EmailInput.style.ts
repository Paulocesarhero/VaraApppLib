import { StyleSheet } from "react-native";

export const emailInputStyle = StyleSheet.create({
  input: {
    backgroundColor: "#fff",
    borderWidth: 1,
    height: 50,
    borderRadius: 12,
    textAlign: "center",
    fontSize: 20,
  },
});
