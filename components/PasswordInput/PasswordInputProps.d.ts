import { TextInputProps } from "react-native";

/**
 * Propiedades para el componente de entrada de contraseña (`PasswordInput`).
 *
 * Esta interfaz extiende las propiedades predeterminadas de `TextInput` en React Native, permitiendo
 * que el componente de entrada de contraseña (`PasswordInput`) herede todas las propiedades disponibles
 * para un campo de entrada de texto.
 *
 * @interface PasswordInputProps
 * @extends TextInputProps
 *
 */
export interface PasswordInputProps extends TextInputProps {}
